//Accept a number from the user and check whether the number is odd or even.

#include<stdio.h>

int main()
{
   int i;

  printf("\n Enter the Number = ");
  scanf(" %d", &i);

   if(i % 2 == 0 )
      printf("\n %d Is the even number ", n);
   else
      printf("\n %d Is the odd number ", n);

   return 0;
}
